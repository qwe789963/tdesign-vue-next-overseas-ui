---
description: Vite + Vue 3 + NestJS 项目代码规范和文件组织规则（主规范）
alwaysApply: true
enabled: true
updatedAt: 2025-12-31T00:00:00.000Z
---

## 📌 重要说明

**本文件是项目的主规范文件，所有其他规范文件必须遵循本文件的要求。**

- 补充规范：`TypeScript官方规范.md`、`TypeScript编程开发指南.md`、`CSS官方规范.md`
- 执行指南：`UNIFIED_RULES.md`
- 当规范冲突时，以本文件为准

---

## 【必须】AI 编码工作流程规范

### 先分析后编码原则
每次用户提出需求或对话时，**必须**遵循以下工作流程：

1. **复述阶段**（必须首先执行）
   - 对用户输入的需求进行**完整复述**
   - 确保 AI 正确理解用户意图
   - 复述格式：
     ```
     📋 **需求复述**：
     [用简洁清晰的语言复述用户需求的核心内容]
     ```

2. **分析阶段**（必须先完成）
   - 理解用户需求，分析涉及的文件和模块
   - 评估技术方案和实现路径
   - 输出**分析报告**，包含：
     - 需求理解摘要
     - 涉及的文件/模块列表
     - 技术方案说明
     - 预估影响范围
     - 风险点（如有）

3. **方案输出阶段**（必须在编码前完成）
   - 输出详细的实现方案
   - 方案格式：
     ```
     📐 **实现方案**：
     1. [步骤1]
     2. [步骤2]
     ...
     ```

4. **确认阶段**（必须等待用户确认）
   - 将分析报告和方案呈现给用户
   - **禁止**在用户确认前修改任何代码
   - 等待用户明确表示"可以"、"确认"、"开始"、"同意"等同意信号
   - 询问用户决策：
     ```
     ❓ **请确认**：以上方案是否符合您的预期？确认后我将开始实现。
     ```

5. **编码阶段**（用户确认后执行）
   - 按照确认的方案进行编码
   - 遵循本规范的所有编码要求

### SPEC 需求特殊流程
对于 `specs/` 目录下的 SPEC 需求文档，**必须**遵循以下增强流程：

1. **阅读 SPEC 文档**：完整阅读 spec.md 及相关文件
2. **复述需求**：用自己的语言复述 SPEC 中的核心需求
3. **输出方案**：给出详细的技术实现方案
4. **等待确认**：**禁止**在用户确认前修改任何代码
5. **执行实现**：用户确认后开始编码

### 禁止占位符和未实现代码
编码过程中**严格禁止**以下情况：

- ❌ `// TODO: 待实现`
- ❌ `// FIXME: 需要修复`
- ❌ `// ... 其他代码`
- ❌ `throw new Error('Not implemented')`
- ❌ `pass  # 占位符`
- ❌ `/* 此处省略实现 */`
- ❌ 任何形式的占位符、伪代码或未完成的实现

**要求**：
- 所有代码必须是**完整可运行**的实现
- 如果某个功能无法在当前阶段完成，必须在分析报告中说明，而不是用占位符代替
- 每个函数、方法必须有完整的实现逻辑

## 规范说明

每项规范内容的要求等级定义：
- **必须（Mandatory）**：代码必须遵循，违反此规范应视为错误
- **推荐（Preferable）**：代码应当遵循，但在特殊情况下可以例外，不视为错误
- **可选（Optional）**：可以参考，根据具体情况决定是否采用

---


## 【必须】代码规范

### 通用规范
- 每个方法、类、属性、函数等都必须有标准JSDOC注释
- 访问修饰符必须加上`public`、`private`、`protected`等关键字
- 代码中禁止使用`alert()`
- 需要添加OOP思想，体现继承、多态、封装
- 每一行结尾必须以`;`结束，文件末尾必须有换行符
- vue页面文件尽量依据功能结构划分代码结构，公共的组件组件存放在`src/components/` 目录，当前模块、页面级别的组件存放在自已文件存放目录的`components/` 子目录
- 多个以上页面或者模块、组件、或函数功能等应该抽象成公共组件和迁移存放目录到公共components
- 所有代码中涉及到显示到前端页面静态字段内容需要加入语言包，如 `t('stock')`、`t('user')`，默认是英文


### 命名规范
- **类名**：大写字母开头（PascalCase），如 `UserService`、`AuthGuard`
- **方法名**：小写字母开头（camelCase），如 `getUserInfo()`、`validateToken()`
- **属性名**：小写字母开头（camelCase），如 `userId`、`isActive`
- **函数名**：小写字母开头（camelCase），如 `formatDate()`、`parseConfig()`
- **接口定义**：以`I`开头（PascalCase），如 `IUserService`、`IAuthStrategy`
- **常量**：全大写（UPPER_SNAKE_CASE），如 `MAX_RETRY_COUNT`、`DEFAULT_TIMEOUT`
- **枚举**：PascalCase，如 `UserRole`、`AuthStatus`

---

## 【必须】非说明文件命名规范

### 通用规则
- 文件名**必须**以小写形式命名，不能出现大写字母
- 文件名不能超过30个字符
- 文件名不能包含空格
- 使用连字符（`-`）分隔单词，不使用下划线或驼峰命名
- 禁止使用变量形式的命名，如 `indexDS.js`，应为 `index-db.js`

### 前缀命名规则（Vue 3 客户端）
- **Vue 组件**：`component-*.vue`，如 `component-header.vue`、`component-stock-card.vue`
- **Composables**：`composables-*.ts`，如 `composables-api.ts`、`composables-i18n.ts`
- **Stores**：`store-*.ts`，如 `store-auth.ts`、`store-stocks.ts`
- **Services（接口请求）**：`*-*.service.ts`，如 `user-api.service.ts`、`stock-data.service.ts`（放在 `services/` 目录）
- **Utils（工具函数）**：`utils-*.ts`，如 `utils-security.service.ts`、`utils-performance.service.ts`（放在 `utils/` 目录）
- **Types**：`types-*.ts`，如 `types-stock.ts`
- **Styles**：`styles-*.less`，如 `styles-app.less`、`styles-variables.less`
- **Icons**：`icon-*.png`，如 `icon-add.png`、`icon-home.png`

### 前缀命名规则（NestJS 服务端）
- **配置文件**：`config-*.ts`，如 `config-app.ts`、`config-database.ts`、`config-dev.ts`、`config-prod.ts`
- **实体类**：`entity-*.ts` 或 `*.entity.ts`，如 `entity-stock.ts`、`user.entity.ts`
- **仓储类**：`repository-*.ts`，如 `repository-stock.ts`
- **Schema**：`*.schema.ts`，如 `user.schema.ts`、`portfolio.schema.ts`
- **DTO**：`*.dto.ts`，如 `auth.dto.ts`、`user.dto.ts`
- **Utils**：`utils-*.ts`，如 `utils-alpha-vantage.service.ts`
- **Services**：`*-*.service.ts` 或 `*.service.ts`，如 `stock-data.service.ts`、`hunyuan-ai.service.ts`
- **Guards**：`*-*.guard.ts`，如 `jwt-auth.guard.ts`
- **Strategies**：`*.strategy.ts`，如 `jwt.strategy.ts`、`local.strategy.ts`

### 测试文件命名规范 - 【必须】

#### 命名格式
测试文件**必须**使用以下格式命名：`{目录前缀}-{功能名}.spec.ts`

#### 目录前缀规则
| 源文件目录 | 测试文件前缀 | 示例 |
|-----------|-------------|------|
| `composables/` | `composables-` | `composables-idempotent-request.spec.ts` |
| `services/` | `services-` | `services-figma-make.spec.ts` |
| `utils/` | `utils-` | `utils-security.spec.ts` |
| `modules/auth/` | `auth-` | `auth-service.spec.ts` |
| `modules/shared/` | `shared-` | `shared-utils-logger.spec.ts` |

#### 命名示例
```
# ✓ 正确命名
composables-idempotent-request.spec.ts   # composables 目录下的测试
services-figma-make.spec.ts              # services 目录下的测试
utils-figma-parser.spec.ts               # utils 目录下的测试
auth-service.spec.ts                     # auth 模块的服务测试
shared-utils-logger.spec.ts              # shared 模块的工具测试

# ✗ 错误命名
idempotent-request.spec.ts               # 缺少目录前缀
figmaMake.spec.ts                        # 使用驼峰命名
FigmaParser.test.ts                      # 使用 PascalCase 和 .test 后缀
```

---

## 【必须】目录结构和 index 文件规范

### 测试目录结构规范 - 【必须】

#### 核心原则
测试文件**必须**集中管理在 `__tests__/` 目录下，按功能模块分子目录组织。

#### 前端测试目录结构（Vue 3 客户端）
```
apps/agent/
├── src/
│   ├── composables/
│   ├── services/
│   └── utils/
└── __tests__/                           # 测试根目录
    ├── composables/                     # composables 测试
    │   └── composables-idempotent-request.spec.ts
    ├── services/                        # services 测试
    │   ├── services-figma-make.spec.ts
    │   └── services-figma-template.spec.ts
    └── utils/                           # utils 测试
        ├── utils-figma-parser.spec.ts
        └── utils-security.spec.ts
```

#### 后端测试目录结构（NestJS 服务端）
```
apps/server/
├── src/
│   └── modules/
│       ├── auth/
│       ├── shared/
│       └── email-template/
└── __tests__/                           # 测试根目录
    ├── shared/                          # shared 模块测试
    │   └── shared-utils-logger.spec.ts
    ├── auth/                            # auth 模块测试
    │   └── auth-service.spec.ts
    └── email-template/                  # email-template 模块测试
        ├── email-template-controller.spec.ts
        └── email-template-service.spec.ts
```

#### 测试配置要求

**Vitest 配置（前端）**：
```typescript
// vitest.config.ts
export default defineConfig({
  test: {
    include: ['__tests__/**/*.spec.ts'],
    // ...
  }
});
```

**Jest 配置（后端）**：
```javascript
// jest.config.js
module.exports = {
  testMatch: ['<rootDir>/__tests__/**/*.spec.ts'],
  // ...
};
```

#### 禁止事项
- ❌ 禁止在源码目录（`src/`）中放置测试文件
- ❌ 禁止使用 `*.test.ts` 后缀（统一使用 `*.spec.ts`）
- ❌ 禁止测试文件命名不带目录前缀

### 模块目录结构规范 - 【推荐】

#### 扁平化模块设计
推荐使用 `modules/` 目录组织功能模块，采用扁平化设计：

```
src/modules/
├── home/
│   ├── home-page.vue              # 页面组件
│   ├── components/                # 模块私有组件
│   │   └── component-hero.vue
│   └── index.ts                   # 导出文件
├── auth/
│   ├── auth-page.vue
│   ├── login-page.vue
│   └── register-page.vue
├── email-template/
│   ├── email-template-page.vue
│   ├── email-editor-page.vue
│   ├── components/
│   │   ├── component-template-list.vue
│   │   └── component-email-preview.vue
│   └── services/
│       └── email-template.service.ts
└── prompt-views/
    ├── prompt-detail.vue          # 扁平化：直接放在模块根目录
    ├── prompt-history.vue
    └── vue-prompt-editor.vue
```

#### 模块 vs views 目录选择
| 场景 | 推荐目录 | 说明 |
|------|---------|------|
| 功能模块（含组件、服务） | `modules/` | 完整功能单元 |
| 简单页面（仅页面组件） | `views/` | 纯展示页面 |
| 路由嵌套页面 | `views/` | 传统路由结构 |

### 单组件目录规则
**定义**：目录中只有一个 Vue 组件文件的情况

**规则**：
- 必须使用 `index.vue` 作为文件名
- 不能同时存在 `index.ts` 和 `index.vue`
- 目录名使用小写连字符格式

**示例**：
```
views/
├── auth/
│   ├── login/
│   │   └── index.vue          ✓ 正确
│   └── register/
│       └── index.vue          ✓ 正确
├── health/
│   └── index.vue              ✓ 正确
└── portfolio/
    └── index.vue              ✓ 正确
```

### 多组件/导出目录规则
**定义**：目录中有多个组件或需要统一导出的情况

**规则**：
- 必须使用 `index.ts` 作为导出文件
- `index.ts` 中**只能包含 export 语句**，不能包含业务逻辑
- 不能同时存在 `index.ts` 和 `index.vue`
- 每个组件使用带前缀的独立文件名

**示例**：
```
components/
├── component-header.vue       ✓ 独立组件
├── component-stock-card.vue   ✓ 独立组件
└── index.ts                   ✓ 导出文件（仅包含 export）

stores/
├── store-auth.ts              ✓ 独立 store
├── store-stocks.ts            ✓ 独立 store
└── index.ts                   ✓ 导出文件（仅包含 export）

modules/auth/strategies/
├── jwt.strategy.ts            ✓ 独立策略
├── local.strategy.ts          ✓ 独立策略
└── index.ts                   ✓ 导出文件（仅包含 export）
```

### index.ts 文件内容规范
- **只能包含**：`export` 语句
- **禁止包含**：类定义、函数实现、业务逻辑
- **示例**：
```typescript
// ✓ 正确
export { JwtStrategy } from './jwt.strategy';
export { LocalStrategy } from './local.strategy';

// ✗ 错误 - 不能包含类定义
export class JwtStrategy {
  // ...
}
```

### 特殊目录规则

#### 样式目录 (styles/)
```
styles/
├── index.less                 ✓ 主入口，导入所有样式
├── app/
│   └── index.less             ✓ 应用级样式
├── components/
│   └── index.less             ✓ 组件样式
├── stocks/
│   └── index.less             ✓ 功能模块样式
├── styles-variables.less      ✓ 变量定义
└── styles-tdesign-override.less ✓ 第三方库覆盖
```

#### 国际化目录 (locales/)
```
locales/
├── index.ts                   ✓ 导出文件
├── en/
│   └── index.ts               ✓ 英文翻译
└── zh-CN/
    └── index.ts               ✓ 中文翻译
```

#### 资源目录 (assets/)
```
assets/
├── icons/
│   ├── icon-add.png           ✓ 带前缀的图标
│   ├── icon-home.png
│   └── icon-settings.png
```

#### 前端服务目录 (services/) - 【必须】
**定义**：所有接口请求、网络通信相关的服务文件

**规则**：
- 所有接口请求service文件**必须**放在 `services/` 目录
- 文件命名：`*-*.service.ts`，如 `user-api.service.ts`、`stock-data.service.ts`
- 每个service文件对应一个业务模块或API端点
- 必须使用 `index.ts` 作为导出文件，仅包含 export 语句
- 禁止在 `utils/` 目录中放置接口请求相关的service

**示例**：
```
services/
├── user-api.service.ts        ✓ 用户相关API
├── stock-data.service.ts      ✓ 股票数据API
├── portfolio-api.service.ts   ✓ 投资组合API
├── auth-api.service.ts        ✓ 认证API
└── index.ts                   ✓ 导出文件（仅包含 export）
```

#### 前端工具目录 (utils/) - 【必须】
**定义**：所有工具函数、辅助函数、非网络请求相关的服务

**规则**：
- 工具函数service文件**必须**放在 `utils/` 目录
- 文件命名：`utils-*.ts`，如 `utils-security.service.ts`、`utils-performance.service.ts`
- 包括：安全工具、性能优化、可靠性、可扩展性等工具类
- 禁止在此目录放置接口请求相关的service

**示例**：
```
utils/
├── utils-api.service.ts       ✓ API客户端配置（非接口请求）
├── utils-security.service.ts  ✓ 安全工具（XSS防护、加密等）
├── utils-performance.service.ts ✓ 性能优化（缓存、防抖等）
├── utils-reliability.service.ts ✓ 可靠性（日志、错误监控）
├── utils-scalability.service.ts ✓ 可扩展性（权限、角色管理）
├── utils-decimal.service.ts   ✓ 数值处理工具
└── index.ts                   ✓ 导出文件（仅包含 export）
```

#### NestJS 模块目录
```
modules/auth/
├── auth.controller.ts         ✓ 控制器
├── auth.service.ts            ✓ 服务
├── auth.module.ts             ✓ 模块
├── dto/
│   └── auth.dto.ts            ✓ 数据传输对象
├── guards/
│   └── jwt-auth.guard.ts       ✓ 独立守卫
├── strategies/
│   ├── jwt.strategy.ts         ✓ 独立策略
│   ├── local.strategy.ts       ✓ 独立策略
│   └── index.ts                ✓ 导出文件
```

---

## 【必须】代码安装运行

### 开发命令
- 启动开发服务器：`npm run dev`
- 构建生产版本：`npm run build`
- 运行测试：`npm run test`

### 变更验证流程 - 【必须】

每次代码变更后，**必须**按以下顺序执行验证：

#### 1. TypeScript 类型检查
```bash
# 前端项目
npx vue-tsc --noEmit

# 后端项目
npx tsc --noEmit
```

#### 2. 单元测试
```bash
# 前端（Vitest）
npm run test

# 后端（Jest）
npm run test
```

#### 3. ESLint 检查
```bash
npm run lint
```

#### 4. 验证清单
| 检查项 | 命令 | 必须通过 |
|--------|------|---------|
| TypeScript 编译 | `npx tsc --noEmit` | ✅ |
| 单元测试 | `npm run test` | ✅ |
| ESLint | `npm run lint` | ✅ |
| 构建验证 | `npm run build` | ✅（发布前） |

#### 5. 提交前检查
建议配置 `husky` + `lint-staged` 自动执行：
```json
// package.json
{
  "lint-staged": {
    "*.{ts,vue}": ["eslint --fix", "prettier --write"],
    "*.spec.ts": ["vitest related --run"]
  }
}
```

---

## 【必须】语言包规范

### 多语言支持
- 代码中新增绑定内容**必须**考虑加入语言包
- 所有用户可见的文本都应该使用国际化（i18n）

### 语言包位置
- 前端：`项目目录/src/locales/{lang}/index.ts`
- 支持语言：`en`（英文）、`zh-CN`（简体中文）

### 使用规范
- 在 Vue 组件中使用 `$t()` 函数获取翻译
- 在 TypeScript 中使用 `i18n.t()` 获取翻译
- 新增翻译时同时更新所有语言文件

---

## 【推荐】代码组织最佳实践

### 导入顺序
1. 外部库导入（如 Vue、NestJS）
2. 内部模块导入（相对路径）
3. 类型导入（`import type`）

### 文件大小
- 单个文件不超过 500 行代码
- 超过 500 行应该拆分为多个文件

### 注释规范
- 使用 JSDOC 格式注释所有公开接口
- 复杂逻辑添加行内注释说明意图

---

## 【必须】数据库操作规范

### 核心原则
数据库操作需要从四个层面全面考虑：**安全性与完整性**、**性能与效率**、**可维护性与可靠性**、**扩展性与架构**

### 安全性与完整性
- **SQL注入防范**：必须使用参数化查询（Prepared Statements），禁止直接拼接SQL
- **权限管理**：遵循最小权限原则，不同用户分配最小必需权限
- **数据加密**：敏感信息（密码、个人身份信息）必须加密存储
- **事务控制**：关键操作必须使用事务保证ACID特性

### 性能与效率
- **查询优化**：避免SELECT *，只获取需要的字段；避免全表扫描
- **索引策略**：根据实际查询需求创建合适的索引，定期审查和优化
- **连接池管理**：使用数据库连接池复用连接，防止连接泄漏
- **缓存机制**：对热点数据使用Redis/Memcached等内存缓存

### 可维护性与可靠性
- **规范化设计**：遵循第三范式（3NF）减少数据冗余
- **定期备份**：制定定期备份策略，定期进行恢复演练
- **监控日志**：开启慢查询日志，监控数据库运行状态
- **软件更新**：定期更新DBMS及其补丁

### 扩展性与架构
- **读写分离**：当读远大于写时，采用主从复制架构
- **分库分表**：单表数据量巨大时，考虑水平或垂直分表
- **数据库选择**：根据业务特点选择合适的数据库类型（关系型/NoSQL）

---

## 【必须】前端安全规范

### 防范注入攻击（XSS）
- **输入验证**：对所有用户输入进行严格验证和过滤，采用"白名单"原则
- **输出转义**：将数据输出到页面前进行转义处理，或使用textContent替代innerHTML
- **内容安全策略（CSP）**：配置CSP HTTP头，明确允许加载的资源域名

### 保障通信安全
- **HTTPS强制**：全程使用HTTPS，通过HSTS头强制浏览器加密通信
- **CSRF防护**：关键操作使用CSRF Token验证，确保请求源自应用本身
- **Cookie安全**：为Cookie设置SameSite=Strict或Lax属性

### 依赖与敏感数据管理
- **依赖扫描**：定期使用npm audit扫描项目依赖，及时更新有漏洞的库
- **敏感信息保护**：禁止在前端代码、LocalStorage、Cookie中硬编码或明文存储API密钥、令牌
- **数据加密**：必须暂存敏感信息时，应进行加密处理

### 权限控制与监控
- **RBAC实现**：实施基于角色的访问控制，动态渲染菜单和按钮
- **错误监控**：集成Sentry等工具实时捕获运行时异常和安全事件
- **安全审计**：定期进行安全审计和代码审查

### 安全工具集成（必须）
前端代码必须使用统一的安全工具库，**所有安全相关函数必须从 `@tencent/s2-core-lib` 包获取**，禁止各工程自行实现：

> ⚠️ **重要**：安全工具统一由 `@tencent/s2-core-lib` 包提供，确保安全实现的一致性和可维护性。
> - **Vue 3 项目**：`npm install @tencent/s2-core-lib@^1.6.2 -S`
> - **Vue 2 项目**：`npm install @tencent/s2-client-vue-lib@^1.7.6 -S`

#### XSS 防护
- **HTML 转义**：使用 `escapeHtml()` 替代自行实现的转义函数
- **HTML 净化**：使用 `sanitizeHtml()` 净化用户输入的富文本
- **纯文本净化**：使用 `sanitizeText()` 净化用户输入
- **URL 净化**：使用 `sanitizeUrl()` 验证和清理 URL
- **XSS 风险检测**：使用 `hasXssRisk()` 检测潜在 XSS 攻击

```typescript
// ✓ 正确 - Vue 3 项目从 @tencent/s2-core-lib 导入安全工具
import { escapeHtml, sanitizeHtml, sanitizeText, sanitizeUrl, hasXssRisk } from '@tencent/s2-core-lib';

const safeContent = escapeHtml(userInput);
const safeHtml = sanitizeHtml(richText, ['b', 'i', 'a', 'p']);
const safeUrl = sanitizeUrl(userUrl);

// ✓ 正确 - Vue 2 项目从 @tencent/s2-client-vue-lib 导入
import { escapeHtml, sanitizeText } from '@tencent/s2-client-vue-lib';

// ✗ 错误 - 禁止自行实现
const escapeHtml = (str) => str.replace(/[<>&"']/g, ...); // 禁止
// ✗ 错误 - 禁止从项目本地导入
import { escapeHtml } from '@/utils/security'; // 禁止
```

#### 防重复提交（防抖/节流）
- **输入防抖**：使用 `useDebounceFn()` 或 VueUse 的 `useDebounceFn`
- **滚动节流**：使用 `useThrottleFn()` 或 VueUse 的 `useThrottleFn`
- **表单提交锁**：自定义 `useSubmitLock()` composable

```typescript
// ✓ 正确 - 使用 VueUse 的防抖节流
import { useDebounceFn, useThrottleFn } from '@vueuse/core';

const debouncedSearch = useDebounceFn((query: string) => {
  // 搜索逻辑
}, 300);

const throttledScroll = useThrottleFn(() => {
  // 滚动处理逻辑
}, 100);

// ✓ 正确 - 自定义提交锁 composable
const useSubmitLock = () => {
  const isSubmitting = ref(false);
  const withLock = async (fn: () => Promise<void>) => {
    if (isSubmitting.value) return;
    isSubmitting.value = true;
    try {
      await fn();
    } finally {
      isSubmitting.value = false;
    }
  };
  return { isSubmitting, withLock };
};
```

---

## 【必须】后端安全规范

> ⚠️ **重要**：后端安全工具统一由 `@tencent/s2-core-node` 包提供，确保安全实现的一致性和可维护性。
> 安装：`npm install @tencent/s2-core-node@^1.6.0 -S`

### 请求限流
- **全局限流**：所有 API 默认启用限流守卫 `RateLimitGuard`
- **敏感接口限流**：登录、注册等敏感接口使用严格限流

```typescript
// ✓ 正确 - 从 @tencent/s2-core-node/nest 导入限流装饰器和配置
import { RateLimit, RateLimitGuard, LOGIN_RATE_LIMIT, STRICT_RATE_LIMIT, STANDARD_RATE_LIMIT } from '@tencent/s2-core-node/nest';

@UseGuards(RateLimitGuard)
@Controller('api')
export class ApiController {
  @RateLimit(LOGIN_RATE_LIMIT)  // 每分钟 5 次
  @Post('login')
  async login() {}

  @RateLimit(STRICT_RATE_LIMIT)  // 每分钟 10 次
  @Post('sensitive-action')
  async sensitiveAction() {}
}

// ✗ 错误 - 禁止从项目本地导入
import { RateLimit } from '@/common'; // 禁止
```

### CSRF 防护
- **状态变更接口**：POST/PUT/DELETE 接口必须启用 CSRF 保护
- **Token 验证**：使用 `CsrfGuard` 验证请求来源

```typescript
// ✓ 正确 - 从 @tencent/s2-core-node/nest 导入 CSRF 装饰器
import { CsrfGuard, CsrfProtection, SkipCsrf } from '@tencent/s2-core-node/nest';

@UseGuards(CsrfGuard)
@Controller('api')
export class ApiController {
  @CsrfProtection({ checkReferer: true })
  @Post('update-profile')
  async updateProfile() {}

  @SkipCsrf()  // 仅用于公开 API 或 Webhook
  @Post('webhook')
  async webhook() {}
}

// ✗ 错误 - 禁止从项目本地导入
import { CsrfProtection } from '@/common'; // 禁止
```

### 输入验证
- **DTO 验证**：所有接口必须使用 DTO + class-validator 验证
- **安全检测**：启用 `EnhancedValidationPipe` 自动检测 SQL 注入和 XSS

```typescript
// ✓ 正确 - 从 @tencent/s2-core-node/nest 导入验证管道
import { EnhancedValidationPipe, StrictValidationPipe } from '@tencent/s2-core-node/nest';

// 全局启用增强验证管道
app.useGlobalPipes(new EnhancedValidationPipe({
  whitelist: true,
  forbidNonWhitelisted: true,
  detectSqlInjection: true,
  detectXss: true,
}));

// 或使用预设的严格验证管道
app.useGlobalPipes(StrictValidationPipe);
```

### 安全服务使用
- **Token 生成**：使用 `SecurityService.generateToken()`
- **CSRF Token**：使用 `SecurityService.generateCsrfToken()`
- **幂等键**：使用 `SecurityService.generateIdempotencyKey()`
- **请求签名**：使用 `SecurityService.generateRequestSignature()`
- **数据脱敏**：使用 `SecurityService.maskPhone/Email/IdCard()`

```typescript
// ✓ 正确 - 从 @tencent/s2-core-node/nest 导入安全服务
import { SecurityService } from '@tencent/s2-core-node/nest';

@Injectable()
export class MyService {
  constructor(private readonly security: SecurityService) {}

  async createOrder() {
    const idempotencyKey = this.security.generateIdempotencyKey('order');
    const signature = this.security.generateRequestSignature('POST', '/api/order', Date.now(), body, secret);
    // ...
  }
}

// ✗ 错误 - 禁止从项目本地导入
import { SecurityService } from '@/modules/security'; // 禁止
```

### HTTP 服务基类
所有后端 HTTP 服务应继承 `BaseHttpService`：

```typescript
// ✓ 正确 - 从 @tencent/s2-core-node/nest 导入 HTTP 服务基类
import { BaseHttpService, BaseHttpServiceConfig, ILogger } from '@tencent/s2-core-node/nest';

@Injectable()
export class MyApiService extends BaseHttpService {
  constructor() {
    super({
      baseURL: 'https://api.example.com',
      timeout: 10000,
      serviceName: 'MyApiService',
    });
  }
}
```

### 数据库安全
- **参数化查询**：必须使用 TypeORM 参数化查询，禁止拼接 SQL
- **敏感数据加密**：密码使用 bcrypt，敏感字段使用 AES 加密
- **审计日志**：关键操作必须记录审计日志

---

## 【必须】前端数值计算规范

### 核心原则
前端涉及**金融数据、价格计算、百分比运算**等场景时，**必须使用高精度数值计算工具**，禁止直接使用 JavaScript 原生浮点数运算，避免精度丢失问题。

> ⚠️ **重要**：数值计算工具统一由以下包提供：
> - **Vue 3 项目**：`npm install @tencent/s2-core-lib@^1.6.2 -S`
> - **Vue 2 项目**：`npm install @tencent/s2-client-vue-lib@^1.7.6 -S`

### 为什么需要高精度计算
JavaScript 使用 IEEE 754 双精度浮点数，存在精度问题：
```typescript
// ✗ 错误 - 原生浮点数精度问题
0.1 + 0.2 // 0.30000000000000004
0.3 - 0.1 // 0.19999999999999998
1.005 * 100 // 100.49999999999999
```

### 高精度计算工具使用

#### 基础运算
- **加法**：使用 `decAdd()`
- **减法**：使用 `decSub()`
- **乘法**：使用 `decMul()`
- **除法**：使用 `decDiv()`
- **四舍五入**：使用 `toFixed()`

```typescript
// ✓ 正确 - Vue 3 项目从 @tencent/s2-core-lib 导入高精度计算工具
import { decAdd, decSub, decMul, decDiv, toFixed, formatAmount, calcPercentage } from '@tencent/s2-core-lib';

// 基础运算
const sum = decAdd(0.1, 0.2); // 0.3
const diff = decSub(0.3, 0.1); // 0.2
const product = decMul(1.005, 100); // 100.5
const quotient = decDiv(10, 3); // 3.333...

// 四舍五入
const price = toFixed(123.456, 2); // 123.46

// 格式化金额
const formatted = formatAmount(1234.5, 2, '$'); // "$1,234.50"

// 百分比计算
const percent = calcPercentage(25, 100, 2); // 25.00

// ✓ 正确 - Vue 2 项目从 @tencent/s2-client-vue-lib 导入
import { decAdd, decSub, decMul, decDiv, toFixed } from '@tencent/s2-client-vue-lib';

// ✗ 错误 - 禁止直接使用原生运算
const sum = 0.1 + 0.2; // 禁止
const product = price * quantity; // 禁止（涉及金额计算时）
```

#### 格式化输出
```typescript
// ✓ 正确 - 格式化数值
import { toFixed, formatAmount, calcPercentage } from '@tencent/s2-core-lib';

const price = toFixed(123.456, 2); // 123.46 (number)
const currency = formatAmount(1234.5, 2, '$'); // "$1,234.50"
const percent = calcPercentage(25, 100, 2); // 25.00

// ✗ 错误 - 禁止使用原生 toFixed（存在精度问题）
const price = (123.456).toFixed(2); // 禁止
```

### 适用场景
以下场景**必须**使用高精度计算：
1. **股票价格**：涨跌幅、市值、成交额
2. **财务数据**：收入、利润、资产
3. **订单金额**：商品价格、折扣、税费
4. **投资组合**：持仓市值、盈亏计算
5. **汇率转换**：货币换算
6. **百分比计算**：占比、增长率

### 禁止事项
```typescript
// ✗ 以下写法均被禁止

// 1. 直接使用原生运算符进行金额计算
const total = price * quantity;
const discount = price * 0.9;

// 2. 使用原生 toFixed
const formatted = value.toFixed(2);

// 3. 直接比较浮点数
if (a === b) { }

// 4. 使用 Math 函数处理金额
const rounded = Math.round(price * 100) / 100;

// 5. 从项目本地导入
import { DecimalService } from '@/utils/decimal'; // 禁止
```

---

## 【必须】前端性能规范

### 资源加载优化
- **代码压缩**：使用Webpack/Vite进行代码压缩、Tree Shaking
- **图片优化**：压缩图片并使用WebP等现代格式
- **懒加载**：延迟加载非关键资源（如图片），使用preload预加载关键资源
- **CDN加速**：通过CDN和HTTP/2/3提升网络传输效率

### 渲染效率优化
- **骨架屏**：使用骨架屏提升用户感知上的加载速度
- **SSR/SSG**：对于内容型网站，考虑服务端渲染或静态站点生成
- **代码分割**：实施代码分割和动态导入（如React.lazy()），按需加载
- **虚拟滚动**：对于长列表，使用虚拟滚动技术只渲染可视区域

### 运行时性能优化
- **防抖/节流**：对频繁触发的事件（滚动、输入）使用防抖和节流
- **动画优化**：使用requestAnimationFrame，优先使用CSS transform和opacity
- **缓存策略**：静态资源使用强缓存，数据接口使用协商缓存

### 性能监控
- **Lighthouse评分**：定期使用Lighthouse进行综合评分和优化建议
- **Performance分析**：利用Chrome DevTools Performance面板分析性能瓶颈
- **Core Web Vitals**：在线上环境监控LCP、FID、CLS等关键指标

---

## 【必须】文档规范

### 文档位置
- 新生成的 `.md` 说明文件放在 `/docs` 目录下
- 遵循【必须】总结报告及说明书规范中的命名规范

### 文档内容
- 包含项目概述、安装步骤、使用说明
- 更新时同步修改文档版本号

### SPECS相关
- 变更人：`@yourname`
- 变更类型：`feat`/`fix`/`docs`/`style`/`refactor`/`perf`/`test`/`chore`
- 变更内容：`[文件名]`/`[文件名]: [变更内容]`
- 变更原因：`[变更原因]`
- 变更时间：`[yyyy-mm-dd]`
> 变更人为当前提交者用户名

---

## 【必须】Git 提交规范

### 提交消息格式
MUST 使用语义化提交消息，遵循 Conventional Commits 规范：

**格式**: `<type>(<scope>): <description>`

- `type`: 提交类型（必须）
- `scope`: 影响范围（可选）
- `description`: 简短描述（必须）

### 提交类型

| 类型       | 说明                      | 示例 |
| ---------- | ------------------------- | ---- |
| `feat`     | 新增功能                  | `feat(auth): add login page` |
| `fix`      | 修复 BUG                  | `fix(api): fix token refresh` |
| `docs`     | 修改文档                  | `docs: update README` |
| `style`    | 样式修改 (不影响代码逻辑) | `style: format code` |
| `refactor` | 代码重构                  | `refactor(utils): simplify logic` |
| `perf`     | 性能优化                  | `perf(list): add virtual scroll` |
| `test`     | 测试相关                  | `test(auth): add unit tests` |
| `build`    | 构建相关                  | `build: update webpack config` |
| `ci`       | CI/CD 相关                | `ci: add github actions` |
| `chore`    | 杂项更改                  | `chore: update dependencies` |
| `revert`   | 回滚提交                  | `revert: revert commit abc123` |
| `types`    | 类型定义更改              | `types(user): add IUser interface` |

### 提交规范要求
- 提交消息 MUST 使用英文
- 描述 MUST 以小写字母开头
- 描述 MUST 使用祈使语气（如 `add` 而非 `added`）
- 描述 SHOULD NOT 超过 72 个字符
- 如有 Breaking Change，MUST 在 body 中说明

---

## 【必须】总结报告及说明书规范

### 命名规范
- 文件名：`[项目名称]-[版本号]-[变更类型]-[变更内容]-[变更原因].md`
- 文件夹名：`[项目名称]-[版本号]`

### 文件内容
- 项目概述：项目背景、项目目标、项目范围
- 安装步骤：项目安装、项目启动、项目测试
- 使用说明：项目使用、项目配置、项目部署
- 变更记录：变更类型、变更内容、变更原因、变更时间
- 附录：项目依赖、项目配置、项目部署

### 变更类型
- `feat`：新功能
- `fix`：修复BUG
- `docs:`：文档更新
- `style`：代码格式更新
- `refactor`：代码重构
- `perf`：性能优化
- `test`：测试用例更新
- `chore`：构建过程或辅助工具的变动

### 变更内容
- `[文件名]`：文件名
- `[文件名]: [变更内容]`：文件名及变更内容
- `[变更人：@yourname]`
- `[变更原因]`：变更原因
