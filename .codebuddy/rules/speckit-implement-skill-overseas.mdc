---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-03-04T09:19:45.877Z
provider: 
---

# 海外组件 Skill 触发条件（overseas-component-adapter）

## 规则优先级：最高

本规则适用于**所有对话场景**，不仅限于 `speckit.implement`。

## 触发条件（满足任意一项即触发）

### 1. 用户消息关键字扫描

每次收到用户新消息时，**首先**扫描消息内容是否包含以下关键字（不区分大小写）：

- 海外、overseas、海外样式、overseas style、海外组件、overseas component
- overseas adaptation、overseas UI、海外适配、海外版

### 2. Spec/Tasks 文件关键字扫描

执行 `speckit.implement` 或类似实现命令时，在读取 `implementation/tasks.md` 和 `spec.md` 文件**之后**，检查标题、任务描述、故事名称中是否包含上述关键字。

### 3. 文件路径匹配

任务涉及创建或修改以下目录的文件：

- `style/overseas/` 目录
- `overseas/` 海外样式目录
- `packages\components` 组件目录

## 执行流程

1. **第一步（最先执行）**：检测到任意触发条件 → **立即**调用 `use_skill("overseas-component-adapter")`
2. **第二步**：等待 skill 加载完成
3. **第三步**：按照 skill 定义的工作流程进行后续操作（分析、编码等）

## 禁止事项

- ❌ **禁止**在未加载 skill 的情况下对海外组件进行任何代码分析或输出分析报告
- ❌ **禁止**在未加载 skill 的情况下对海外组件进行任何代码修改
- ❌ **禁止**以"先分析再加载"为由延迟 skill 加载
- ❌ **禁止**在匹配条件满足时跳过 skill 加载直接编码

## 注意事项

- 若 skill 在当前对话中已加载，则无需重复加载
- 此规则优先于其他工作流程规则执行